<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="action_export_edicom" model="ir.actions.server">
        <field name="name">Exportar a Ediwin</field>
        <field name="model_id" ref="account.model_account_move" />
        <field name="binding_model_id" ref="account.model_account_move" />
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code">records.action_export_edicom()</field>
    </record>

    <record id="view_account_move_form" model="ir.ui.view">
        <field name="name">account.move.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <xpath
                expr="//field[@name='invoice_line_ids']/tree/field[last()]"
                position="after"
            >
                <field name="edicom_dept" optional="hide" />
                <field
                    name="edicom_buyer_id"
                    widget="res_partner_many2one"
                    context="{'res_partner_search_mode': (context.get('default_move_type', 'entry') in ('out_invoice', 'out_refund', 'out_receipt') and 'customer') or (context.get('default_move_type', 'entry') in ('in_invoice', 'in_refund', 'in_receipt') and 'supplier') or False,                                             'show_address': 1, 'default_is_company': True, 'show_vat': True}"
                    domain="[('company_id', 'in', (False, company_id))]"
                    options="{'no_quick_create': True, 'no_create': True, 'no_create_edit': True}"
                    optional="hide"
                />
                <field name="edicom_original_order" optional="hide" />
            </xpath>
            <xpath expr="//field[@name='partner_shipping_id']" position="after">
                <field
                    name="edicom_partner_id"
                    widget="res_partner_many2one"
                    invisible="move_type not in ('out_invoice', 'out_refund')"
                    readonly="state != 'draft'"
                    options="{'no_quick_create': True, 'no_create': True, 'no_create_edit': True}"
                />
            </xpath>
        </field>
    </record>
</odoo>
